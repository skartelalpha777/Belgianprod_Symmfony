<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>
      {% block title %}
        Epfc Production
      {% endblock %}
    </title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>" />

    {# --- 1. CSS --- #}
    {# On utilise le CDN officiel Bootstrap 5.3 #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />

    <link rel="stylesheet" href="{{ asset('assets/css/stylebase.css') }}" />
    {% block stylesheets %}

    {% endblock %}

    {% block javascripts %}

    {% endblock %}
  </head>
  <body>
    <header>
      <div class="row navigation py-4">
        <div class="col-8 logo">🎬 EpfcProduction</div>

        <div class="col-4">
          <nav class="navbar navbar-expand-lg">
            <div class="container-fluid">
              <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <li class="nav-item px-4 rounded-pill">
                    <a class="nav-link" aria-current="page" href="{{ path('app_film_index') }}">Films</a>
                  </li>

                  <li class="nav-item px-4 rounded-pill">
                    <a class="nav-link" aria-current="page" href="{{ path('app_acteur_index') }}">Acteurs</a>
                  </li>

                  <li class="nav-item px-4 rounded-pill">
                    <a class="nav-link" href="{{ path('app_categorie_index') }}">Categories</a>
                  </li>
                  {% if not app.user %}
                    <li class="nav-item px-4 rounded-pill">
                      <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                    </li>
                  {% else %}
                    <li class="nav-item px-4 rounded-pill">
                      Connecté en tant que:  {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
                    </li>
                  {% endif %}
                </ul>
              </div>
            </div>
          </nav>
        </div>
      </div>
    </header>

    {% block body %}

    {% endblock %}

    <footer>
      <div class="row footer">
        <div class="col-sm-6 mb-3 mb-sm-0 g-0">
          <div class="card footerCard h-100">
            <div class="card-body">
              <p class="card-title fs-2 text">Contact</p>
              <p class="card-text">✉️ contact@cinemanager.com.</p>
              <p class="card-text">📞 +32 123 456 789.</p>
              <p class="card-text">📍 Antwerpen, Belgique.</p>
              <p class="card-text">📠 +32 2 123 45 67</p>
            </div>
          </div>
        </div>
        <div class="col-sm-6 g-0">
          <div class="card footerCard h-100">
            <div class="card-body">
              <p class="card-title fs-2 text">EpfcProduction</p>
              <p href class="card-text">© 2024 CinéManager. Tous droits réservés.</p>
              <p>
                <a href="" class="card-text">Mentions légales.</a>
              </p>
              <p>
                <a href="" class="card-text">Politique de confidentialité</a>
              </p>
              <p>
                <a href="" class="card-text">CGU</a>
              </p>
            </div>
          </div>
        </div>
      </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
